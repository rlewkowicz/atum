---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: platformidentityconfigurations.platform.atum.dev
spec:
  group: platform.atum.dev
  names:
    kind: PlatformIdentityConfiguration
    listKind: PlatformIdentityConfigurationList
    plural: platformidentityconfigurations
    shortNames:
    - atumidentity
    singular: platformidentityconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PlatformIdentityConfigurationSpec is the closed declaration of the Keycloak
              and Vault provider objects owned by Atum. Provider endpoints and Kubernetes
              credential names are fixed by the controller and are not extension points.
            properties:
              domain:
                maxLength: 253
                minLength: 3
                pattern: ^(?:[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\.)+[a-z]{2,63}$
                type: string
                x-kubernetes-validations:
                - message: domain is immutable after provider ownership is established
                  rule: self == oldSelf
              keycloak:
                properties:
                  administrator:
                    properties:
                      group:
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z][a-z0-9-]{0,62}$
                        type: string
                      realmRole:
                        enum:
                        - admin
                        type: string
                      username:
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z][a-z0-9-]{0,62}$
                        type: string
                    required:
                    - group
                    - realmRole
                    - username
                    type: object
                  clients:
                    items:
                      properties:
                        audience:
                          type: boolean
                        id:
                          pattern: ^[a-z][a-z0-9-]{0,62}$
                          type: string
                        kind:
                          enum:
                          - PublicPKCE
                          - Confidential
                          type: string
                        redirectURIs:
                          items:
                            type: string
                          maxItems: 16
                          minItems: 1
                          type: array
                          uniqueItems: true
                        webOrigins:
                          items:
                            type: string
                          maxItems: 16
                          type: array
                          uniqueItems: true
                      required:
                      - id
                      - kind
                      - redirectURIs
                      type: object
                    maxItems: 32
                    minItems: 1
                    type: array
                    x-kubernetes-list-map-keys:
                    - id
                    x-kubernetes-list-type: map
                  groupsScope:
                    properties:
                      claimName:
                        enum:
                        - groups
                        type: string
                      name:
                        enum:
                        - atum-groups
                        type: string
                    required:
                    - claimName
                    - name
                    type: object
                  realm:
                    enum:
                    - master
                    type: string
                  scopes:
                    items:
                      enum:
                      - openid
                      - profile
                      - email
                      - groups
                      type: string
                    maxItems: 4
                    minItems: 4
                    type: array
                    uniqueItems: true
                    x-kubernetes-validations:
                    - message: scopes must be openid, profile, email, groups in canonical
                        order
                      rule: self == ['openid', 'profile', 'email', 'groups']
                required:
                - administrator
                - clients
                - groupsScope
                - realm
                - scopes
                type: object
              vault:
                properties:
                  authPath:
                    enum:
                    - oidc
                    type: string
                  externalGroup:
                    properties:
                      claim:
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z][a-z0-9-]{0,62}$
                        type: string
                      name:
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z][a-z0-9-]{0,62}$
                        type: string
                      policyName:
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z][a-z0-9-]{0,62}$
                        type: string
                    required:
                    - claim
                    - name
                    - policyName
                    type: object
                  policy:
                    properties:
                      name:
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z][a-z0-9-]{0,62}$
                        type: string
                      purpose:
                        enum:
                        - PlatformAdministration
                        type: string
                    required:
                    - name
                    - purpose
                    type: object
                  role:
                    properties:
                      clientID:
                        pattern: ^[a-z][a-z0-9-]{0,62}$
                        type: string
                      groupsClaim:
                        enum:
                        - groups
                        type: string
                      name:
                        enum:
                        - atum-admin
                        type: string
                      redirectURIs:
                        items:
                          type: string
                        maxItems: 16
                        minItems: 1
                        type: array
                        uniqueItems: true
                      scopes:
                        items:
                          enum:
                          - openid
                          - profile
                          - email
                          - groups
                          type: string
                        maxItems: 4
                        minItems: 4
                        type: array
                        uniqueItems: true
                    required:
                    - clientID
                    - groupsClaim
                    - name
                    - redirectURIs
                    - scopes
                    type: object
                required:
                - authPath
                - externalGroup
                - policy
                - role
                type: object
            required:
            - domain
            - keycloak
            - vault
            type: object
            x-kubernetes-validations:
            - message: vault.role.clientID must name one declared confidential client
              rule: self.keycloak.clients.exists(c, c.id == self.vault.role.clientID
                && c.kind == 'Confidential')
            - message: Vault role scopes must equal the canonical Keycloak scopes
              rule: self.vault.role.scopes == self.keycloak.scopes
            - message: Vault external group policyName must match the declared policy
              rule: self.vault.externalGroup.policyName == self.vault.policy.name
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 3
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
                x-kubernetes-validations:
                - message: conditions are limited to KeycloakReady, VaultReady, and
                    Ready
                  rule: self.all(c, c.type in ['KeycloakReady', 'VaultReady', 'Ready'])
                - message: condition messages must not exceed 512 characters
                  rule: self.all(c, size(c.message) <= 512)
              failureCount:
                format: int32
                maximum: 6
                minimum: 0
                type: integer
              observedGeneration:
                format: int64
                minimum: 0
                type: integer
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: PlatformIdentityConfiguration must use the singleton name atum
          rule: self.metadata.name == 'atum'
    served: true
    storage: true
    subresources:
      status: {}
